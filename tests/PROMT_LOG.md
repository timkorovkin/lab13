# Prompt Log — Лабораторная работа 13

## Task 1 — Развернуть NATS-сервер в Docker
Инструмент: Claude (claude.ai)
Промпт: "Разверни NATS-сервер в Docker для использования в многоагентной системе"

Что сделано:
- Запущен NATS-сервер командой `docker run -d --name nats-server -p 4222:4222 nats:latest`
- Исправлена ошибка с WSL — выполнен `wsl --unregister docker-desktop`, перезапущен Docker Desktop
- NATS доступен на порту 4222

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: перезапуск Docker Desktop после ошибки WSL
- Время: ~15 мин

---

## Task 2 — Разработка прототипа агента на Go
Инструмент: Claude (claude.ai)
Промпт: "Реализовать агента на Go, который подписывается на очередь NATS, принимает JSON с резюме, определяет уровень кандидата и публикует результат"

Что сделано:
- Создан `agent/resume_parser.go` — агент подписывается на тему `resume.parse`, парсит JSON с резюме, определяет уровень кандидата (junior/middle/senior) по количеству лет опыта, публикует результат в `resume.parsed`
- Создан `main.go` — точка входа, подключается к NATS и запускает агента
- Инициализирован Go-модуль: `go mod init lab13`, установлена зависимость `github.com/nats-io/nats.go`
- Результат: агент запускается, подключается к NATS и ожидает задачи

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: исправить пустой `go.mod` через `go mod edit -module=lab13`, понизить версии зависимостей до совместимых с Go 1.24
- Время: ~20 мин

---

## Task 3 — Разработка оркестратора на Python
Инструмент: Claude (claude.ai)
Промпт: "Создать оркестратор на Python с asyncio и nats-py, который отправляет задачи агенту и получает результаты"

Что сделано:
- Создан `orchestrator/orchestrator.py` — подключается к NATS, отправляет резюме в тему `resume.parse`, получает результаты из `resume.parsed`
- Создан `orchestrator/requirements.txt` — зависимости: nats-py
- Установлена библиотека: `pip install nats-py`
- Результат: оркестратор успешно отправляет 3 резюме, агент обрабатывает и возвращает уровни junior/middle/senior

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

---

## Task 4 — Настройка коммуникации через NATS и Docker Compose
Инструмент: Claude (claude.ai)
Промпт: "Развернуть NATS и оба сервиса в Docker Compose с общей сетью"

Что сделано:
- Создан `docker-compose.yml` — три сервиса (nats, go-agent, orchestrator) в общей сети `hr-network`
- Создан `Dockerfile.agent` — сборка Go-агента
- Создан `orchestrator/Dockerfile.orchestrator` — сборка Python-оркестратора
- Обновлены `main.go` и `orchestrator.py` — NATS URL берётся из переменной окружения `NATS_URL`
- Результат: `docker-compose up --build` поднимает все сервисы, сообщения доставляются корректно

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: подбор совместимых версий Go и зависимостей (golang:1.24-alpine, понижение версий crypto, compress)
- Время: ~30 мин

---

## Task 5 — Логирование и мониторинг
Инструмент: Claude (claude.ai)
Промпт: "Добавить логирование в агента и оркестратор с уровнями INFO и ERROR, вывод в консоль и файл, счётчик обработанных задач"

Что сделано:
- Обновлён `agent/resume_parser.go` — логи пишутся одновременно в консоль и `logs/agent.log`, добавлен атомарный счётчик обработанных задач
- Обновлён `orchestrator/orchestrator.py` — логи пишутся в консоль и `logs/orchestrator.log`, добавлен счётчик задач
- Результат: логи корректно записываются в файлы, счётчик отображается после каждой задачи

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: удалить лишнюю функцию `setupLogger` которая вызывала ошибку компиляции
- Время: ~20 мин

---

## Task 6 — Обработка ошибок и таймаутов
Инструмент: Claude (claude.ai)
Промпт: "Реализовать механизм повторной отправки задачи при сбое агента (не более 3 раз) и таймаут ожидания результата"

Что сделано:
- Обновлён `orchestrator/orchestrator.py` — добавлена функция `send_with_retry` с таймаутом 5 секунд и максимум 3 попытками
- При таймауте выводится ERROR-лог с номером попытки, после 3 неудач задача пропускается
- Результат: проверено запуском оркестратора без агента — корректно выводит таймауты и повторные попытки

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

---

## Task 7 — Запуск нескольких агентов и балансировка нагрузки
Инструмент: Claude (claude.ai)
Промпт: "Запустить несколько экземпляров Go-агента и продемонстрировать балансировку нагрузки через NATS"

Что сделано:
- В `agent/resume_parser.go` заменён `Subscribe` на `QueueSubscribe` с группой `resume-workers`
- Запущены два экземпляра агента одновременно
- Результат: задачи распределились между агентами — первый обработал Иванова и Петрову, второй Сидорова

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~10 мин

---

## Task 8 — REST API на FastAPI
Инструмент: Claude (claude.ai)
Промпт: "Разработать REST API на FastAPI, который принимает HTTP запросы и передаёт задачи агентам через NATS"

Что сделано:
- Создан `api/main.py` — FastAPI с эндпоинтами `POST /parse-resume` и `GET /health`
- API подключается к NATS при старте, отправляет резюме агентам и ждёт результат с таймаутом 5 секунд
- Установлены зависимости: `pip install fastapi uvicorn`
- Результат: API проверено через Swagger UI на `http://127.0.0.1:8000/docs`, резюме обрабатываются корректно

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~20 мин

---

## Task 9 — Тестирование
Инструмент: Claude (claude.ai)
Промпт: "Написать юнит-тесты для агента на Go и оркестратора на Python"

Что сделано:
- Создан `tests/agent_test.go` — 6 Go-тестов для функции `DetermineLevel`, проверяют граничные значения (0, 1, 3, 4, 5, 10 лет опыта)
- Создан `tests/test_orchestrator.py` — 2 pytest-теста с моками NATS: успешная обработка и таймаут
- Установлены зависимости: `pip install pytest pytest-asyncio`
- Результат: все тесты прошли — 6/6 Go, 2/2 Python

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~20 мин

---

## Task 10 — Документирование архитектуры
Инструмент: Claude (claude.ai)
Промпт: "Создать диаграмму взаимодействия компонентов и описание архитектуры системы"

Что сделано:
- Создан `docs/architecture.md` — описание всех компонентов системы, диаграмма Mermaid с sequence diagram, таблица топиков NATS, команды запуска
- Результат: документация отображается на GitHub с визуальной диаграммой

Итого:
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~10 мин